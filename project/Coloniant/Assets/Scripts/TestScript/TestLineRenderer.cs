using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using NUnit.Framework;
//using UnityEngine.TestTools;

public class TestLineRenderer {

    // Line Renderer Lets player see path of travel
	/*[Test]
    public void TestLineRendererTest()
    {
        List<Waypoint> waypointList = new List<Waypoint>();
        waypointList.Add(WaypointManager.main.SpawnWaypoint(WaypointManager.WaypointType.LEAF_SITE, WaypointManager.Level.ABOVE_GROUND, null, new Vector3(0, 0, 0)));
        WaypointManager.main.SpawnWaypoint(WaypointManager.WaypointType.ENTRANCE, WaypointManager.Level.ABOVE_GROUND, null, new Vector3(1, 1, 1));
        Assert.IsNotNull(GameObject.FindObjectOfType<LineRenderer>());
    }*/

}
